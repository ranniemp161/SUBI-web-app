# Review, uncommitted, 2026-07-07

**Reviewed by**: self (author on self)
**Scope**: 6 files, uncommitted
**Verdict**: Approve

## Summary
The changes successfully migrate the rate limiting logic from PostgreSQL to Vercel KV (Upstash Redis) across both `rough-cut` and `wallet` apps. The `rateLimits` table has been cleanly dropped from the Drizzle schema, and the cron cleanup endpoint was updated to reflect this. The code is clean, handles local development gracefully when environment variables are missing, and is fully tested. 

## Minor
### 🟡 Rate-limiter cache, `apps/wallet/src/lib/rate-limit.ts:12`
**Problem**: The `ratelimiters` map caches Ratelimit instances by `limit` and `windowSeconds`. However, `Redis.fromEnv()` is evaluated on initialization, which might be fine for Next.js API routes, but be mindful of serverless environment variable lifetimes.
**Why it matters**: It's a small edge case if the deployment environment resets env vars but retains the container, but generally Next.js restarts the process.
**Suggested fix**: Keep it as is, but monitor if `fromEnv()` causes issues on Vercel Edge.

## Strengths
- **Graceful Fallback**: The `!limiter` check allowing requests when `KV_REST_API_URL` is missing is a very nice touch to keep local development frictionless.
- **Cleanup Refactor**: The Stripe webhook logging improvements and DB schema cleanup are cleanly executed.
- **Robust testing**: The unit tests were appropriately updated and test coverage is comprehensive across both apps.

## Test coverage
The logic is fully covered. The test subagents successfully added and passed 29 tests across `rate-limit.ts`, `proxy.ts`, `cron/cleanup/route.ts`, and `webhooks/stripe/route.ts`. There are no missing test coverage areas for the introduced code.
